'''
AI for Decision-Making 
Defence Innovation Network Project
''' 

Use these requirements text files to get the correct packages to run each notebook.

For the following notebooks: end2end, intro_streaming_bookshelf, sensor_errors and temporal_shifts, use the command 
''' 
pip install -r end2end_requirements.txt
'''

when inside the requirements directory. 

For the benchmark notebooks 1DConv_bookshelf_heli, Bookshelf_Benchmarks and Bookshelf_Feature_Extraction_SVM_NN, use the command 
'''
pip install -r tf_requirements.txt
'''

